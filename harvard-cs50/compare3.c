// Print string char by char

#include <stdio.h>
#include <string.h>

bool compare_strings(char *a, char *b);

int main(void) {

    char s[10], t[10];
    if (compare_strings(s, t)) {
        printf("same\n");
    } else {
        printf("different\n");
    }
}
bool compare_strings(char *a[], char *b[]) {
    if strlen(*a) != strlen()
};
